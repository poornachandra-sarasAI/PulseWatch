[project]
name = "pulsewatch-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27,<1.0",
    "pydantic>=2.0,<3.0",
]

[tool.pyright]
venvPath = ".."
venv = ".venv"
pythonVersion = "3.11"
